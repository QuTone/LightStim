# Runtime dependencies — kept in sync with pyproject.toml [dependencies]
numpy>=1.20.0
stim>=1.15.0
sinter>=1.15.0
PyMatching>=2.0.0
pandas

# Optional decoder back-ends (pyproject.toml extras: decoders, gpu)
stimbposd
mwpf
frozendict
frozenlist
